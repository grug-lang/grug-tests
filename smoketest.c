#include "tests.h"

// TODO: Update tests.sh to run this, where clang instead of gcc must still be pickable too:
// gcc smoketest.c tests.c -Wall -Wextra -pedantic -Werror -Wfatal-errors -fsanitize=address,undefined -g -lm

// TODO: Call the functions in tests.h.
int main(void) {
}
